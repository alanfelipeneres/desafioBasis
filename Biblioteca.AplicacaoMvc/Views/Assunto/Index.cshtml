@model IEnumerable<Biblioteca.AplicacaoMvc.Models.AssuntoVM>

@{
    ViewData["Title"] = "Gerenciar Assuntos";
}

<div class="container">
    <h1 class="display-4 text-center">Gerenciar Assuntos</h1>
    <div class="mb-3 text-end">
        <!-- Botão para adicionar um novo assunto -->
        <a asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Adicionar Novo Assunto
        </a>
    </div>

    @if (Model != null && Model.Any())
    {
        <!-- Tabela para exibir os assuntos -->
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Descrição</th>
                    <th style="width: 300px;">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var assunto in Model)
                {
                    <tr>
                        <td>@assunto.CodAs</td>
                        <td>@assunto.Descricao</td>
                        <td>
                            <!-- Botão para detalhes -->
                            <a asp-action="Details" asp-route-id="@assunto.CodAs" class="btn btn-info btn-sm">
                                <i class="bi bi-eye"></i> Detalhes
                            </a>
                            <!-- Botão para edição -->
                            <a asp-action="Edit" asp-route-id="@assunto.CodAs" class="btn btn-primary btn-sm">
                                <i class="bi bi-pencil"></i> Editar
                            </a>
                            <!-- Botão para exclusão -->
                            <a asp-action="Delete" asp-route-id="@assunto.CodAs" class="btn btn-danger btn-sm">
                                <i class="bi bi-trash"></i> Excluir
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <!-- Mensagem caso não haja assuntos cadastrados -->
        <div class="alert alert-warning text-center">
            Não há assuntos cadastrados no momento.
        </div>
    }
</div>

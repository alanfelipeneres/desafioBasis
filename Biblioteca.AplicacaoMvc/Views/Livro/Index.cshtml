@model IEnumerable<Biblioteca.AplicacaoMvc.Models.LivroVM>

@{
    ViewData["Title"] = "Gerenciar Livros";
}

<div class="container">
    <h1 class="display-4 text-center">Gerenciar Livros</h1>
    <div class="mb-3 text-end">
        <!-- Botão para adicionar um novo Livro -->
        <a asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Adicionar Novo Livro
        </a>
    </div>

    @if (Model != null && Model.Any())
    {
        <!-- Tabela para exibir os Livros -->
        <table class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Título</th>
                    <th>Editora</th>
                    <th style="width: 300px;">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var livro in Model)
                {
                    <tr>
                        <td>@livro.CodL</td>
                        <td>@livro.Titulo</td>
                        <td>@livro.Editora</td>
                        <td>
                            <!-- Botão para detalhes -->
                            <a asp-action="Details" asp-route-id="@livro.CodL" class="btn btn-info btn-sm">
                                <i class="bi bi-eye"></i> Detalhes
                            </a>
                            <!-- Botão para edição -->
                            <a asp-action="Edit" asp-route-id="@livro.CodL" class="btn btn-primary btn-sm">
                                <i class="bi bi-pencil"></i> Editar
                            </a>
                            <!-- Botão para exclusão -->
                            <a asp-action="Delete" asp-route-id="@livro.CodL" class="btn btn-danger btn-sm">
                                <i class="bi bi-trash"></i> Excluir
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <!-- Mensagem caso não haja Livros cadastrados -->
        <div class="alert alert-warning text-center">
            Não há livros cadastrados no momento.
        </div>
    }
</div>
